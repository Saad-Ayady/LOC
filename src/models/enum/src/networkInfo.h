#ifndef INFO_NETWORKINFO_H
#define INFO_NETWORKINFO_H

void getNetworkAdapters();    // RIGHT: Declaration only (no {})
void getIPConfig();           // RIGHT: Declaration only (no {})
void getOpenPorts();          // RIGHT: Declaration only (no {})
void getFirewallStatus();     // RIGHT: Declaration only (no {})
void getAllNetworkInfo();     // RIGHT: Declaration only (no {})

#endif // INFO_NETWORKINFO_H



